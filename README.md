# 🔐 Code Verification App

**Code Verification** — это UI-модуль для Flutter-приложений, реализующий экран подтверждения по СМС/Email коду. Проект включает в себя ввод PIN-кода, таймер обратного отсчета для повторной отправки и экран успешной верификации.

## ✨ Основные возможности

* **🔢 Ввод PIN-кода**: Удобное поле для ввода цифр (4 знака) с автоматическим фокусом и стилизацией.
* **⏱️ Таймер повторной отправки**: Виджет с обратным отсчетом (например, 60 секунд) перед тем, как кнопка "Отправить код повторно" станет активной.
* **✅ Валидация**: Проверка введенного кода и переход на экран успеха (`SuccessScreen`) при правильном вводе.
* **📱 Адаптивный UI**: Чистый и современный интерфейс, понятный пользователю.

## 🛠 Технологический стек

* **Фреймворк**: [Flutter](https://flutter.dev/) (Dart)
* **Архитектура**: Использование `StatefulWidget` для управления локальным состоянием (таймер, ввод текста).
* **Виджеты**: Кастомные компоненты для ввода (`PinCodeInput`) и управления таймером (`ResendCodeWidget`).

## 📂 Структура проекта

Проект разбит на переиспользуемые виджеты и экраны:

```text
lib/
├── screens/
│   ├── verification_screen.dart # Главный экран ввода кода
│   └── success_screen.dart      # Экран успешной верификации
├── widgets/
│   ├── pin_code_input.dart      # Виджет для посимвольного ввода цифр
│   └── resend_code_widget.dart  # Виджет с таймером обратного отсчета
└── main.dart                    # Точка входа
